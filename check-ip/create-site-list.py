"""
Tiny program to process a text file that has Alexa sites in it, and then create another file with just the site names
"""

import os


def main():
    try:
        with open("dumpsites.txt") as dumpster:
            for 
    except:
        print(
            "Create a file called 'dumpsites.txt' and copy paste any top site page from alexa.com into it."
        )


if __name__ == "__main__":
    main()
